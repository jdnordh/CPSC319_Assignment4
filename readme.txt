To run, open the file Assign4.java, anduse the command line arguments:
input.txt query.txt output1.txt output2.txt

Where input.txt is an adjacency matrix, and query.txt is the sequence 
of nodes for which connectiong paths should be obtained